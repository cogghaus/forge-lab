# Forge-lab — session handoff (2026-05-30)

Paste the prompt below into a fresh session to continue.

---

## CONTINUATION PROMPT

You are continuing work on **forge-lab** (G:\dev\forge-lab, monorepo, branch main).
Read these memory files first: `project_forge_lab_fm_roadmap.md` (source of truth)
and `reference_accserver_runner.md`. Key context:

**What's live (verified 2026-05-30):**
- Hub + dash at https://lab.local.cogg.haus, MCP at https://mcp.lab.local.cogg.haus/mcp —
  all on **accserver = 192.168.66.220** (NOT accskynet/.226).
- **All 9 agent daemons running** as Docker containers (compose project
  `forge-daemons`, `deploy/daemons.compose.yml`) — forge-fm + 8 workers.
  Pipeline validated end to end (furnace executed a task, claude OAuth-authed,
  wrote output + done marker, hub marked completed).
- On accserver `adam` is in the `docker` group → run plain `docker` over
  `ssh adam@192.168.66.220`, no sudo. Hub admin API at `http://localhost:3000`,
  login `adam@cogg.haus` / `PLACEHOLDER_CHANGE_ME`, workspace
  `KDzHTInHhuzjST8HDRy8x`.
- Daemon ops + first-time rebuild: `docs/runbooks/daemon-deployment-accserver.md`.

**Just fixed (confirm still green):**
- CD runner restart loop (corrupted runner-data) — wiped + re-registered.
- CD env-file failure — runner now bind-mounts `/datapool/docker/vibe-forge` (ro);
  cd.yml pins project `deploy`; removed stray `.claude/worktrees` gitlinks.

**Open items / suggested next work (pick with the user):**
1. **SECURITY**: rotate the GitHub PAT in
   `/datapool/docker/forge-lab/runner-compose.yml` (it was exposed in a prior
   session transcript). And change the hub admin password off the default
   `PLACEHOLDER_CHANGE_ME` (Settings → Account; also update `FORGE_MCP_PASSWORD`
   in `/datapool/docker/vibe-forge/.env` + restart forge-mcp).
2. **OAuth shared-home race**: 9 containers share one writable
   `/home/forge`; under heavy fan-out, refresh-token rotation can collide.
   Evaluate switching the fleet to `ANTHROPIC_API_KEY`.
3. **CD does not deploy daemons** (only hub/dash/mcp). Decide whether to add a
   manual `workflow_dispatch` job that builds + deploys `forge-daemons`.
4. **Exercise the FM triage path**: create a `pending_dispatcher_action` task and
   confirm forge-fm triages → assigns → a worker completes it (only the
   worker-path was smoke-tested so far).
5. **Agent dev-capability**: workers run `claude` in an empty `/workdir` with no
   forge-lab repo/git/push creds — fine for self-contained tasks, but real dev
   tasks need a repo checkout + git identity + push auth in the workdir. Design
   this if agents are meant to open PRs.
6. **Cleanup**: accskynet (.226) has a stray `~/dev/forge-lab` clone + global pm2
   from a wrong-box detour; remove. Test tasks `smoke-001` (stuck in_progress,
   `cancel` returned 500 — investigate that endpoint) and `smoke-002` (completed)
   linger in the workspace.

Start by confirming the daemon fleet is still healthy
(`docker ps --filter label=com.docker.compose.project=forge-daemons`) and the
latest CD run is green, then ask the user which item to tackle.

---

## State at handoff
- main @ (see `git log`) — last commits: daemon Dockerfile+compose, CD fixes, docs.
- 9 daemons up; CD + runner repaired.
