# Forge-lab — session handoff (2026-06-04)

Paste the CONTINUATION PROMPT below into a fresh session to continue.

---

## Status at handoff

**Branch:** main @ `32ee29c` (squash of feat/heimdall-phase2). All work merged.

**Shipped + deployed this session:**

- **PR #120** `fix(daemon)`: FM circuit breaker + spawn cooldown. `fmSpawnAttempts` counter per task; quarantine after `MAX_TRIAGE_ATTEMPTS=3` FM spawns without inbox progress. Per-workspace `fmCooldownMs` (default 60s, env `FORGE_DAEMON_FM_COOLDOWN_MS`). Crashed FM clears cooldown; normal completion keeps it. Vibe-forge 2-round review caught ordering bug (sightings incremented during cooldown = premature quarantine) — fixed before merge. forge-fm rebuilt + restarted on accserver with new image.

- **Lint hotfix**: `fix(daemon)`: unused `meta` params in test loggers — broke CI on PR #120 merge commit. Direct push to main.

- **CD runner recovered**: corrupted runner-data loop (id=29 offline). Deleted stale GH runner, alpine-wiped runner-data, restarted. CD green again.

- **PR #121** `feat(hub)`: Heimdall Phase 2 — DB-backed policy rules.
  - Migration 0011: `policy_rules` table (workspace_id nullable = global, archived_at soft-delete)
  - Engine: `loadDbRules()` merges workspace + global active DB rules with built-ins per request. Non-fatal on load failure.
  - CRUD: `GET/POST /workspaces/:id/policy-rules`, `PATCH /:ruleId` (archive), `GET/POST /policy-rules` (global, admin-only)
  - Validation: principal regex `^(agent|role|user|device):[a-zA-Z0-9*_-]+$`, resourceCondition must be valid JSON, cap 100 rules/workspace (422 on exceed)
  - Device enforcement: `device:deregister` + `device:rotate-token` now call `checkPolicy`. Built-in allows at p50 preserve existing behavior.
  - Dashboard: proxy routes + policy rules section in workspace Settings (list + create form + archive)
  - Vibe-forge 2-round full review (Crucible-X + Aegis + Loki + Slag + Flux). Round 1 blocked on unvalidated resourceCondition + no rule cap + bad principal format. All fixed. Round 2 clean.
  - 480 hub tests pass.

- **PR #72 closed**: stale, superseded by #73.

**Process:**
- Repo ruleset blocks auto-merge — use `gh pr merge <n> --squash --admin` on CI green.
- `git add -A` (then reset docs/qa/), NOT explicit `[id]/(dashboard)` paths — bracket-pathspec aborts git add.
- Version policy: stay 0.8.x patch; no 0.9 without Adam's OK.

---

## Open items (priority order)

1. **SECURITY (Adam's action)** — Revoke the leaked `gho_` OAuth token. GitHub Settings → Applications → Authorized OAuth Apps → revoke "GitHub CLI" → re-run `gh auth login`. Org-admin scope, still live.

2. **OAuth shared-home → ANTHROPIC_API_KEY** — 9 daemons share one writable `/home/forge` (claude credential dir). Concurrent OAuth token refresh can race under load. Fix: provision `ANTHROPIC_API_KEY` per daemon in `deploy/.env.daemons`, update compose to pass it as env var, drop the claude-home volume mount. Cleaner + more stable for a fleet.

3. **Auto-add fleet devices as workspace members on create** — New workspaces don't automatically have fleet device members. Currently manual. Should fire on `POST /workspaces` success.

4. **Daemon in-progress abort** — `POST /workspaces/:id/tasks/:id/cancel` inserts a `taskInstructions` stop signal, but daemon has no instruction polling loop. Running agent never sees the cancel. Phase 3 item.

5. **Heimdall Phase 3** — DB condition evaluator (resourceCondition JSON), policy rule management audit trail (who created/archived what), remaining enforcement points.

---

## CONTINUATION PROMPT

You are continuing **forge-lab** (G:\dev\forge-lab, monorepo, branch main). Read memory files first: `project_forge_lab_fm_roadmap.md` (source of truth — updated this session), `reference_accserver_runner.md` (deploy ops), `feedback_git_add_pathspec.md` + `feedback_versioning.md` (process rules).

**Current state:** forge-lab is fully operational. 9 agent daemons live on accserver (192.168.66.220). Phases A-G + ADR-004 single fleet + Heimdall Phase 1+2 all shipped. Dashboard at https://lab.local.cogg.haus. CD working (runner recovered). Last merged: PR #121 Heimdall Phase 2.

**Open items this session left:**
1. Adam revokes leaked `gho_` token himself (GitHub Settings → Applications → revoke GitHub CLI → re-auth). Chase him on this if not done.
2. Switch daemon fleet from OAuth shared-home to `ANTHROPIC_API_KEY` — provision key per daemon, update `deploy/.env.daemons` + `deploy/daemons.compose.yml` (drop claude-home volume), rebuild + restart fleet. Coordinate with Adam to run on accserver.
3. Auto-add fleet devices as workspace members on workspace create (hub feature).
4. Daemon in-progress abort (instruction polling loop).

Pick up from the open items above, or take direction from Adam on what's next.
