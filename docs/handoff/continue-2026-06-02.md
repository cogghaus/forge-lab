# Forge-lab — session handoff (2026-06-02)

Paste the CONTINUATION PROMPT below into a fresh session to continue.

---

## Status at handoff

**Branch:** main @ `619eb2b`. All work merged + deployed. Dash **v0.8.15** live.

**Shipped + deployed this session (all via CI → admin-merge → CD on accserver):**
- 0.8.3 #105 workspace management (edit repo, archive, delete)
- 0.8.4 #106 workspace view UX overhaul (Pixel)
- 0.8.5 #107 All Tasks + Goal tree polish
- 0.8.6 #108 account row → logout menu (was logging you out on click)
- 0.8.7 #109 agent detail right-rail (?agent=) + dropped mislabeled Devices panel
- 0.8.8 #110 session management (Settings → Active sessions; migration 0010)
- 0.8.9 #111 task detail page theme cleanup (last default-* surface)
- 0.8.10 #112 agent rail Stop + 7-day perf
- 0.8.11 #113 tabbed workspace shell (layout + summary bar + tab rail)
- 0.8.12/0.8.13 #114/#115 left-rail dedup + daemon mgmt → Org + session prune/cap (the 51-session fix)
- 0.8.14 #116 View personality (hub Docker now ships forge-agents personalities/*.md)
- 0.8.15 #117 anvil + furnace personalities ported from vibe-forge (registry now 11). VERIFIED: all 11 .md live in the hub image.
- #118 **ADR-004** `docs/adr/ADR-004-single-fleet-multi-workspace.md` (Status: **Proposed**).

**Dash theme sweep COMPLETE** (no default-*/flat-HeroUI left in the workspace flow). Agent view steps 1–3 done.

**Process notes:**
- Repo ruleset blocks auto-merge — **must `gh pr merge <n> --squash --admin`** on CI green.
- **`git add -A`** (then `git reset docs/qa/`), NOT explicit `[id]`/`(dashboard)` paths — bracket-pathspec aborts the whole add (botched #114; see `feedback_git_add_pathspec.md`). Verify with `git diff --cached --stat`.
- Version policy: stay **0.8.x patch**; no 0.9 without Adam's OK (plenty of feature weight banked).
- Deploy = accserver (`ssh accserver`, adam in docker group, plain docker). Self-hosted runner can flake (`reference_accserver_runner.md`). Verify each CD: `ssh accserver "docker ps --filter name=forge-dash/forge-hub"`.

**Open follow-ups (not started):** repo-switching workers (ADR-004 §3a); auto-add fleet devices as members on workspace create; optional FM sharding; the 0.9 minor bump; stale PR #72 (superseded by #73, can close).

---

## CONTINUATION PROMPT

You are continuing **forge-lab** (G:\dev\forge-lab, monorepo, branch main @ 619eb2b). Read these memory files first: `project_single_fleet.md` (the ADR-004 plan — primary), `project_forge_lab_fm_roadmap.md` (source of truth), `reference_accserver_runner.md` (deploy/runner), `feedback_git_add_pathspec.md` and `feedback_versioning.md` (process rules).

**Goal: take ADR-004 through review, finalize it, then fully implement it.**

1. **2-round vibe-forge team review of `docs/adr/ADR-004-single-fleet-multi-workspace.md`.** Spawn review subagents adopting the relevant vibe-forge personas (Architect, Aegis/security, Crucible/testing, Oracle, Temper/reviewer, Herald/release — same pattern as the earlier Pixel review). 
   - Round 1: each critiques the ADR (correctness, migration safety, repo-bound handling, FM throughput, auth/Heimdall, rollback). Consolidate findings; apply agreed modifications to the ADR.
   - Round 2: re-review the revised ADR; apply final agreed changes.
   - When settled, flip Status: Proposed → **Accepted** and merge.
2. **Full implementation until the ADR is complete.** Per the scope in the ADR:
   - **FM multi-workspace**: config `FORGE_DAEMON_DISPATCHER_SCOPE=single|all`; `HubClient.listWorkspaces()` (reuse GET /workspaces — confirm orchestrator-device auth, else add `GET /dispatcher/workspaces`); `pollDispatcher` loops workspaces when scope=all; dispatcher integration tests (2 workspaces, both inboxes triaged, errors isolated, fmRunning serializes). No schema change.
   - **Worker consolidation migration** (execute on accserver): add the fleet's devices as members of every workspace; unscope workers + set FM scope=all in `deploy/daemons.compose.yml` (drop the `FORGE_DAEMON_WORKSPACE_ID` anchor pin); drain the hal-* set; cut over the single fleet; retire + deregister hal-* duplicates. Rollback path is in the ADR.
   - Handle repo-bound workers per ADR §3 (interim per-repo, or repo-switching if scoped in).
   - Ship as PRs: CI → `gh pr merge --admin` → CD; bump dash patch version each deploy (0.8.x); verify live after each CD.

**Working style:** Adam is on a remote-control session, monitoring and responding. Proceed autonomously through the review + implementation, but surface genuine decisions (ADR modifications that change scope, anything needing Adam to run — though plain docker over `ssh accserver` is available). Use the Monitor tool for CI/CD and report concisely. Caveman mode may be active (terse); write commits/PRs/ADR normally.
